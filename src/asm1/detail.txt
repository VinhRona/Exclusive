import { useState } from "react";
import { Minus, Plus, Truck, RefreshCw, Heart } from "lucide-react";

const ProductDetail = () => {
  const [quantity, setQuantity] = useState(2);
  const [selectedImage, setSelectedImage] = useState("/images/main.jpg");
  const thumbnails = [
    "/img/detai2.jpg",
    "/img/detai3.jpg",
    "/img/detai4.jpg",
    "/img/detai5.jpg",
  ];

  const relatedProducts = [
    
    {
      id: 1,
      name: "HAVIT HV-G92 Gamepad",
      price: "$120",
      oldPrice: "$160",
      discount: "-40%",
      rating: 5,
      reviews: 88,
      image: "/img/detail5.jpg",
    },
    {
      id: 2,
      name: "AK-900 Wired Keyboard",
      price: "$960",
      oldPrice: "$1160",
      discount: "-35%",
      rating: 4,
      reviews: 75,
      image: "/img/detail7.jpg",
    },
    {
      id: 3,
      name: "IPS LCD Gaming Monitor",
      price: "$370",
      oldPrice: "$400",
      discount: "-30%",
      rating: 5,
      reviews: 99,
      image: "/img/detail9.jpg",
    },
    {
      id: 4,
      name: "RGB Liquid CPU Cooler",
      price: "$160",
      oldPrice: "$170",
      discount: "-10%",
      rating: 3,
      reviews: 65,
      image: "/img/detail10.jpg",
    },
  ];

  return (
    <div>

      <div>
        <div className="text-gray-400 text-sm flex items-center space-x-2 p-12 pl-[125px]">
          <span className="hover:text-black cursor-pointer">Account</span>
          <span>/</span>
          <span className="hover:text-black cursor-pointer">Gaming</span>
          <span>/</span>
          <span className="font-semibold text-black">Havic HV G-92 Gamepad</span>
        </div>
      </div>

      <div className="w-[1260px] mx-auto p-0 flex gap-12 ">
      {/* Left: Product Images */}
      <div className="flex flex-row-reverse gap-4 items-start ">
        {/* Main Image */}
        <img
          src="/img/detail.jpg"
          alt="Main Product"
          className="flex-0 w-[1200px] h-[510px] rounded-lg p-2 bg-gray-100 object-contain hover:shadow-lg"
        />

        
        {/* Thumbnail Images */}
        <div className="flex flex-col gap-1">
          {thumbnails.map((src, index) => (
            <img
              key={index}
              src={src}
              alt={`Thumbnail ${index + 1}`}
              className={`hover:shadow-lg w-[280px] h-[125px] rounded-lg cursor-pointer border-2 ${
                selectedImage === src ? "border-blue-500" : "border-transparent"
              }`}
              onClick={() => setSelectedImage(src)}
            />
          ))}
        </div>
      </div>

      {/* Right: Product Details */}
      <div className="">
        <h2 className="text-2xl font-semibold">Havic HV G-92 Gamepad</h2>
        <p className="text-gray-500 text-sm flex items-center gap-2">
          ⭐⭐⭐⭐ (150 Reviews) | <span className="text-green-500">In Stock</span>
        </p>
        <p className="text-2xl font-bold mt-2">$192.00</p>
        <p className="text-gray-500 mt-2">
          PlayStation 5 Controller Skin High quality vinyl with air channel
          adhesive for easy bubble free install.
        </p>

        {/* Colours */}
        <div className="mt-4">
          <p className="font-semibold">Colours:</p>
          <div className="flex gap-2 mt-1">
            <span className="w-5 h-5 bg-gray-300 rounded-full cursor-pointer"></span>
            <span className="w-5 h-5 bg-red-500 rounded-full cursor-pointer"></span>
          </div>
        </div>

        {/* Size Selection */}
        <div className="mt-4">
          <p className="font-semibold">Size:</p>
          <div className="flex gap-2 mt-1">
            {["XS", "S", "M", "L", "XL"].map((size) => (
              <span
                key={size}
                className="px-3 py-1 border rounded-lg cursor-pointer hover:bg-gray-200"
              >
                {size}
              </span>
            ))}
          </div>
        </div>

        {/* Quantity & Buy Button */}
        <div className="mt-4 flex gap-4 items-center">
          <div className="flex items-center border rounded-lg overflow-hidden">
            <button
              className="px-3 py-1 bg-gray-200"
              onClick={() => setQuantity(quantity - 1)}
            >
              <Minus size={16} />
            </button>
            <span className="px-4">{quantity}</span>
            <button
              className="px-3 py-1 bg-gray-200"
              onClick={() => setQuantity(quantity + 1)}
            >
              <Plus size={16} />
            </button>
          </div>
          <button className="bg-red-500 text-white px-6 py-2 rounded-lg hover:bg-green-500">
            Buy Now
          </button>
          <Heart className="cursor-pointer hover:text-red-500" />
        </div>

        {/* Delivery Info */}
        <div className="mt-6 border p-4 rounded-lg">
          <p className="flex items-center gap-2 font-semibold">
            <Truck size={20} /> Free Delivery
          </p>
          <p className="text-sm text-gray-500">
            Enter your postal code for Delivery Availability
          </p>
          <hr className="my-2" />
          <p className="flex items-center gap-2 font-semibold">
            <RefreshCw size={20} /> Return Delivery
          </p>
          <p className="text-sm text-gray-500">
            Free 30 Days Delivery Returns.
          </p>
        </div>
      </div>
    </div>

      <div>
      <div>
    <div className="w-11/12 mx-auto mt-20">
      {/* Title */}
      <div className="flex items-center justify-between">
        <div className="flex items-center space-x-2 pl-16">
          <span className="w-4 h-7 bg-red-500 rounded"></span>
          <h2 className="text-lg font-semibold text-red-500">Related Item</h2>
        </div>
      </div>

      {/* Product List */}
      <div className="grid grid-cols-4 gap-3 mt-12 px-14">
        {relatedProducts.map((product, index) => (
          <div key={index} className=" p-2 rounded-lg relative">

            <a href="/asm/detail">  
              <img src={product.image} alt={product.name} className="w-full object-cover rounded hover:shadow-lg hover:scale-105" />
            </a>
           
            <h3 className="mt-3 text-gray-800 font-semibold">{product.name}</h3>
            <p className="text-red-500 text-lg font-bold">{product.price} <span className="text-gray-400 line-through text-sm">{product.oldPrice}</span></p>
            <div className="flex items-center text-yellow-500">
        {"★".repeat(product.rating)}
        {"☆".repeat(5 - product.rating)}
        <span className="text-gray-400 text-sm ml-2">({product.reviews})</span>
      </div>
          </div>
        ))}
      </div>
    </div>
    </div>
      </div>

     

    </div>
  );
};

export default ProductDetail;
